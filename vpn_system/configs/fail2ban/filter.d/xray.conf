[Definition]
# Match common Xray errors that typically include a source IP.
# We keep it broad because log formats differ between versions/builds.
failregex = ^.*<HOST>.*(rejected|failed|invalid).*$
ignoreregex = 
